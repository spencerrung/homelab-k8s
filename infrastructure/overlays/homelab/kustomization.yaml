---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base/sources
  - ../../base/tf-controller
  - ../../base/vault  # Use base vault (dev mode, no auto-unseal)
  # - ../../base/external-secrets  # Temporarily disabled to fix Vault first
# Add homelab-specific patches and resources here
