---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: alucard-web
  namespace: alucard-web
spec:
  replicas: 2
  selector:
    matchLabels:
      app: alucard-web
  template:
    metadata:
      labels:
        app: alucard-web
    spec:
      containers:
        - name: web
          image: docker.io/spencerrung/alucard-web:latest # {"$imagepolicy": "flux-system:alucard-web"}
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
