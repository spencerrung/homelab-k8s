---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: finale
  namespace: finale
spec:
  replicas: 2
  selector:
    matchLabels:
      app: finale
  template:
    metadata:
      labels:
        app: finale
    spec:
      containers:
        - name: web
          image: docker.io/spencerrung/finale:latest # {"$imagepolicy": "flux-system:finale"}
          ports:
            - containerPort: 80
              name: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
