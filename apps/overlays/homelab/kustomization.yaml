---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
  - ../../alucard-web  # Alucard.dev static website
  - ../../simple-cookbook  # Simple-Cookbook.com static website
  - ../../matrix  # Matrix (Synapse) homeserver + Element web
  - ../../atproto-pds  # AT Protocol / Bluesky PDS (bluesky-social/pds)
  - ../../nova  # WebAssembly showcase (nova.alucard.dev)
  - ../../finale  # WebAssembly fireworks show (250.alucard.dev)
  - ../../breathe  # Paced-breathing guide (breathe.alucard.dev)
# Add homelab-specific patches and resources here
